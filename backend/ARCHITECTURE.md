# 🏗️ Royal Bricks Backend Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                             │
│  (React Frontend / Postman / Mobile App / Third-party APIs)     │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP/HTTPS Requests
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                      SECURITY LAYER                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │  Helmet  │  │   CORS   │  │   Rate   │  │  Input   │       │
│  │ Headers  │  │Protection│  │ Limiting │  │Sanitize  │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
└────────────────────────────┬────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                      MIDDLEWARE LAYER                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │   JWT    │  │  Input   │  │  Error   │  │  Morgan  │       │
│  │   Auth   │  │Validation│  │ Handler  │  │ Logging  │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
└────────────────────────────┬────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                        ROUTING LAYER                             │
│                                                                  │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐ │
│  │   Auth Routes    │  │  Booking Routes  │  │Query Routes  │ │
│  │                  │  │                  │  │              │ │
│  │ POST /register   │  │ POST /bookings   │  │POST /queries │ │
│  │ POST /login      │  │ GET  /bookings   │  │GET  /queries │ │
│  │ GET  /me         │  │ GET  /:id        │  │GET  /:id     │ │
│  │ PUT  /password   │  │ PUT  /:id        │  │PUT  /:id     │ │
│  │                  │  │ DELETE /:id      │  │DELETE /:id   │ │
│  │                  │  │ GET  /stats/*    │  │GET  /stats/* │ │
│  └────────┬─────────┘  └────────┬─────────┘  └──────┬───────┘ │
└───────────┼────────────────────┼────────────────────┼──────────┘
            │                    │                    │
┌───────────▼────────────────────▼────────────────────▼──────────┐
│                     CONTROLLER LAYER                             │
│                                                                  │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐ │
│  │ authController   │  │bookingController │  │queryController│ │
│  │                  │  │                  │  │              │ │
│  │ • register()     │  │ • createBooking()│  │• createQuery()│ │
│  │ • login()        │  │ • getBookings()  │  │• getQueries() │ │
│  │ • getMe()        │  │ • getBooking()   │  │• getQuery()   │ │
│  │ • updatePassword()│ │ • updateBooking()│  │• updateQuery()│ │
│  │                  │  │ • deleteBooking()│  │• deleteQuery()│ │
│  │                  │  │ • getStats()     │  │• getStats()   │ │
│  │                  │  │ • getRevenueByArea()│              │ │
│  └────────┬─────────┘  └────────┬─────────┘  └──────┬───────┘ │
└───────────┼────────────────────┼────────────────────┼──────────┘
            │                    │                    │
┌───────────▼────────────────────▼────────────────────▼──────────┐
│                       MODEL LAYER                                │
│                     (Mongoose Schemas)                           │
│                                                                  │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐ │
│  │   User Model     │  │  Booking Model   │  │ Query Model  │ │
│  │                  │  │                  │  │              │ │
│  │ • email          │  │ • bookingNumber  │  │ • name       │ │
│  │ • password       │  │ • customerName   │  │ • email      │ │
│  │ • fullName       │  │ • phone          │  │ • subject    │ │
│  │ • role           │  │ • brickType      │  │ • message    │ │
│  │ • isActive       │  │ • quantity       │  │ • status     │ │
│  │                  │  │ • area ⭐        │  │ • adminNotes │ │
│  │ Methods:         │  │ • city           │  │              │ │
│  │ • comparePassword│  │ • totalAmount    │  │              │ │
│  │ • hashPassword   │  │ • status         │  │              │ │
│  └────────┬─────────┘  └────────┬─────────┘  └──────┬───────┘ │
└───────────┼────────────────────┼────────────────────┼──────────┘
            │                    │                    │
            └────────────────────▼────────────────────┘
                                 │
┌────────────────────────────────▼────────────────────────────────┐
│                      DATABASE LAYER                              │
│                        MongoDB                                   │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │    users     │  │   bookings   │  │contact_queries│         │
│  │  collection  │  │  collection  │  │  collection  │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                  │
│  Indexes:                                                        │
│  • email (unique)                                               │
│  • bookingNumber (unique)                                       │
│  • status, area, city, createdAt (for fast queries)            │
│  • Aggregation pipelines for analytics                         │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow Example

### Example: Get Revenue by Area

```
1. Client Request
   ↓
   GET /api/bookings/stats/revenue-by-area
   Headers: { Authorization: "Bearer token123..." }

2. Security Layer
   ↓
   ✓ CORS check
   ✓ Rate limit check
   ✓ Input sanitization

3. Middleware Layer
   ↓
   ✓ JWT verification
   ✓ User authentication
   ✓ Role check (admin only)

4. Routing Layer
   ↓
   Route: GET /bookings/stats/revenue-by-area
   → bookingController.getRevenueByArea()

5. Controller Layer
   ↓
   • Parse query parameters
   • Build date filters
   • Call database aggregation

6. Model Layer
   ↓
   • Booking.aggregate([...])
   • Group by area
   • Calculate totals
   • Sort by revenue

7. Database Layer
   ↓
   • Execute aggregation pipeline
   • Use indexes for performance
   • Return results

8. Response
   ↓
   {
     "success": true,
     "data": {
       "revenueByArea": [
         {
           "area": "Patna City",
           "totalRevenue": 2250000,
           "totalOrders": 45,
           "averageOrderValue": 50000
         }
       ]
     }
   }
```

---

## 📊 Analytics Pipeline

```
┌─────────────────────────────────────────────────────────────┐
│                    ANALYTICS ENGINE                          │
└─────────────────────────────────────────────────────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   Revenue    │    │    Order     │    │   Payment    │
│  Analytics   │    │  Analytics   │    │  Analytics   │
└──────────────┘    └──────────────┘    └──────────────┘
        │                    │                    │
        ├─ By Area ⭐        ├─ By Status         ├─ Pending
        ├─ By Brick Type    ├─ By Area           ├─ Partial
        ├─ By City          ├─ By Brick Type     └─ Paid
        ├─ Monthly Trends   └─ Daily Trends
        └─ Total Revenue
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                           │
└─────────────────────────────────────────────────────────────┘

Layer 1: Network Security
├─ CORS (Cross-Origin Resource Sharing)
├─ Helmet (Security Headers)
└─ Rate Limiting (DDoS Protection)

Layer 2: Authentication
├─ JWT Tokens (Stateless Auth)
├─ Password Hashing (bcrypt)
└─ Token Expiration

Layer 3: Authorization
├─ Role-Based Access Control
├─ Protected Routes
└─ Admin-Only Endpoints

Layer 4: Input Security
├─ Input Validation (express-validator)
├─ NoSQL Injection Prevention
└─ Data Sanitization

Layer 5: Error Handling
├─ Centralized Error Handler
├─ No Sensitive Data Leaks
└─ Proper Status Codes
```

---

## 🎯 Key Components

### 1. Authentication System
```
User → Login → JWT Token → Protected Routes → Admin Dashboard
```

### 2. Booking System
```
Customer → Create Booking → Auto Booking# → Admin Review → Update Status
```

### 3. Analytics System
```
Bookings Data → Aggregation → Area/Type/City Analysis → Dashboard Stats
```

### 4. Query System
```
Customer → Submit Query → Admin Review → Update Status → Resolve
```

---

## 📈 Data Flow

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Client  │────▶│   API    │────▶│Controller│────▶│  Model   │
│ (React)  │     │ Routes   │     │  Logic   │     │ (Schema) │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
     ▲                                                     │
     │                                                     ▼
     │                                              ┌──────────┐
     │                                              │ MongoDB  │
     │                                              │ Database │
     │                                              └──────────┘
     │                                                     │
     └─────────────────────────────────────────────────────┘
                    Response with Data
```

---

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      PRODUCTION                              │
└─────────────────────────────────────────────────────────────┘

Frontend (Vercel/Netlify)
    │
    │ HTTPS
    ▼
Backend API (Heroku/Railway/Render)
    │
    │ Secure Connection
    ▼
MongoDB Atlas (Cloud Database)
```

---

## 📊 Performance Optimizations

1. **Database Indexes** - Fast queries on area, status, date
2. **Aggregation Pipelines** - Efficient analytics
3. **Pagination** - Handle large datasets
4. **Compression** - Reduce response size
5. **Connection Pooling** - Reuse database connections

---

This architecture provides:
- ✅ Scalability
- ✅ Security
- ✅ Performance
- ✅ Maintainability
- ✅ Extensibility
